{
  "name": "photography-portfolio",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve _site"
  },
  "dependencies": {
    "image-size": "^2.0.0",
    "marked": "^17.0.0",
    "sharp": "^0.34.0"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "terser": "^5.46.0"
  }
}
